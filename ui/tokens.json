{
  "$comment": [
    "The single source of truth for every colour, size and radius in",
    "Weglet's interface.",
    "",
    "generate_tokens.py turns this into three files: CSS custom properties",
    "for the pages, a TypeScript module for code that needs a value at",
    "runtime, and a C++ header for the window geometry. Nothing else may",
    "declare these numbers.",
    "",
    "This exists because the previous interface kept them in three places",
    "at once -- CSS variables per page, constants in JS, constants in the",
    "window layer -- and keeping the three in step was manual. The toolbar",
    "height in particular has to match between the markup that fills it",
    "and the code that sizes the view holding it, or the bottom is clipped."
  ],
  "color": {
    "surface": "#0a0a0c",
    "surface-raised": "#16161a",
    "surface-sunken": "#050506",
    "border": "#26262c",
    "border-strong": "#3a3a44",
    "text": "#f4f4f6",
    "text-muted": "#9a9aa6",
    "text-faint": "#6a6a76",
    "accent": "#a855f7",
    "accent-hover": "#b975f9",
    "accent-text": "#ffffff",
    "danger": "#f43f5e",
    "warning": "#fbbf24",
    "success": "#22c55e"
  },
  "space": {
    "xs": 2,
    "sm": 4,
    "md": 8,
    "lg": 12,
    "xl": 16,
    "2xl": 24,
    "3xl": 32
  },
  "radius": {
    "sm": 4,
    "md": 8,
    "lg": 12,
    "pill": 999
  },
  "font": {
    "size-xs": 11,
    "size-sm": 12,
    "size-md": 13,
    "size-lg": 16,
    "size-xl": 22,
    "weight-normal": 400,
    "weight-medium": 500,
    "weight-bold": 600
  },
  "layout": {
    "$comment": [
      "Logical pixels throughout -- the same unit CSS uses. The C++ side",
      "converts once, at the boundary, in shell::to_wry_rect's successor.",
      "Getting this wrong is how a 150% display ended up with a clipped",
      "toolbar."
    ],
    "tab-strip-height": 34,
    "toolbar-height": 46,
    "chrome-padding": 8,
    "chrome-gap": 4,
    "tab-min-width": 80,
    "tab-max-width": 220,
    "dock-columns": 4,
    "dock-max-tiles": 8
  },
  "motion": {
    "$comment": ["Milliseconds."],
    "fast": 120,
    "normal": 200,
    "slow": 320
  }
}
